import { WeatherBackgroundDirective } from './weather-background.directive';

describe('WeatherBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
